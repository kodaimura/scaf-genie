services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - APP_ENV=production
      - GENIE_ENV=prod
    working_dir: /app
    command: [ "./entrypoint.sh" ]
    tty: true
